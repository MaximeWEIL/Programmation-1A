#include <stdio.h>

main()
{
	int i; int n = 101; int compteur = 0; int test=0;

	for (i=1 ; i<=n ; i++)
	{
		if (n % i == 0)
		{
			test = test+1;
		}
		
	if (test==2)
	{
		printf("Le nombre %d est premier\n", i);
	}
	}

	return 0;
}
